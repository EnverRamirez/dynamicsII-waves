# dynamicsII-waves
To recreate the environment in which the following examples will run 
```
conda env create -f environment.yml
```
A conda environment named waves must be creadted. To list it:
```
conda env list
```

For environment activation
```
conda activate waves
```
